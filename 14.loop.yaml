- name: loops 
  hosts: local
  connection: local
  tasks:
    - name: print names 
      ansible.bultin.debug:
        msg: "Hello {{ item }}"
      loop:
      - Ramesh
      - Suresh
      - Naresh 